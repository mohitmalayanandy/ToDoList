// Exported as 404.html, which GitHub Pages serves for any path without a
// matching file. Rendering nothing here lets the root layout boot the app and
// resolve the URL client-side, so links to browser-created projects still work.
export default function NotFound() {
  return null;
}
